enable
configure terminal

interface g0/0
 ip address 20.0.0.2 255.0.0.0
 no shutdown
 exit

interface g0/1
 ip address 30.0.0.1 255.0.0.0
 no shutdown
 exit

router rip
 version 2
 no auto-summary
 network 20.0.0.0
 network 30.0.0.0
 exit

write memory
